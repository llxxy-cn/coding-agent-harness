# Reflection

## 1. 项目与个人职责

项目开始时，我把 Coding Agent Harness 简单理解为“自己构建一个 AI 智能体”。随着开发深入，我逐渐认识到，Harness 不只是调用一次大模型，而是要实现上下文构建、模型调用、`Action` 解析、工具执行、结果反馈和停止条件组成的完整循环，同时处理状态持久化、权限、安全与错误恢复。

在项目中，我负责确认需求与架构方向、审核安全边界、控制修改权限，并对 Git 提交和发布进行最终确认。Codex 协助分析要求、生成代码和测试、定位错误及整理文档。它提高了开发效率，但结果是否符合课程要求仍由我判断。

## 2. Superpowers 技能评价

### brainstorming

`brainstorming` 帮助我将模糊想法转化为可验证的规约，使我认识到 Harness 还包含工具、状态和安全边界，但提问过多也会拖慢编码。

### writing-plans

`writing-plans` 将项目拆成二十多个相互依赖的 `Task`，便于检查结构和控制修改范围；但计划过细后，过多时间被用于门禁、汇报和确认。后期改为 Milestone A–D 后，开发过程更加集中。

### test-driven-development

`test-driven-development` 通过 Red、Green 和回归测试明确预期行为，但 AI 生成的测试也可能误解规约，因此测试通过不能替代人工审核。

### systematic-debugging 与 verification-before-completion

`systematic-debugging` 帮助我区分生产缺陷、测试错误和环境差异；`verification-before-completion` 避免 Codex 在未完成验证时过早宣布完成，但也增加了开发时间。

## 3. TDD 在 AI 协作中的作用

`Patch` 补偿测试给我留下了较深印象。当多文件修改中途失败时，系统需要恢复已经修改的文件，但不能覆盖同期发生的外部改动。原实现中文件替换后回调抛出异常，而该文件尚未加入已修改集合，补偿阶段因此错误返回 `RollbackResult(ok=True)`。修复后，系统能够记录真实变更，并在发现外部修改时返回冲突、拒绝覆盖。

我也遇到过测试自身错误的情况。发现测试写错时我感到烦躁，因为不仅要检查实现，还要重新审查测试期望和 `SPEC`。这使我认识到，AI 生成的测试并非天然正确。如果为了让错误测试变绿而修改生产代码，反而可能破坏正确实现。因此，TDD 仍需人工确认 Red 是否合理、测试是否真正表达了需求。

## 4. Task 拆分与 Agent 自主性

细分 `Task` 能够明确文件范围、测试和完成标准，降低单次修改风险；但逐项执行 Red、Green、回归、审核和提交也消耗了较多时间与 token。改为 Milestone A–D 后，核心要求仍被保留，相关功能则能集中实现和验证。

Codex 在编码阶段最可靠，能够快速生成可运行代码、处理我不会实现的模块，并解释模块的大致作用。但这种可靠性依赖明确的范围、测试和停止条件。遇到契约冲突、权限扩大、Windows 沙箱故障或 Git 发布操作时，仍需人工介入。

本项目主要采用单 Agent 串行工作，没有充分使用 `subagent-driven` 工作流，因此无法证明多个 subagent 能够长时间稳定自主运行。我的体验是，Agent 适合在明确边界内工作，而不适合完全无人监督地长期运行。

## 5. SPEC 和 PLAN 对实现的影响

`SPEC` 不仅规定字段和接口，也定义状态关系、失败行为与安全边界。让我印象最深的是，审批变为 `CONSUMED`、`Action` 进入 `EXECUTING` 和执行意图写入必须位于同一个 SQLite 事务。否则系统可能在审批已消费、`Action` 尚未执行时崩溃，留下无法判断能否重试的状态。澄清后，三个操作必须一起成功或回滚。这让我认识到，即使单个函数正确，缺少组合状态的原子性规定仍会产生严重问题。

## 6. Prompt 与 Context 策略

仅告诉 Codex“完成某个功能”通常不够。在不熟悉的问题上，我会先用自然语言说明目标，再让 AI 协助整理成包含文件范围、停止条件和验收标准的提示词，最后再确认是否执行。

明确修改文件、Red 测试、Green 条件和 Git 边界，可以减少 Codex 提前实现后续任务或扩大范围。但提示词过长时，限制之间也可能冲突，使 Codex 花费较多时间核对流程。因此，提示词既要明确，也要控制复杂度。

## 7. 安全、凭据与软件分发

我原以为 AI 生成的 `Patch` 格式正确并能通过测试就可以应用，后来才认识到，通过测试不等于修改安全。Coding Agent 可能修改测试、配置或敏感路径，也可能大量删除源码。因此系统需要在应用前检查路径、修改规模和敏感内容，并决定允许、拒绝或要求审批。只有 `Policy` 返回 `ALLOW` 后，`Patch` 才能修改隔离 worktree，随后还必须运行完整测试。

凭据管理也改变了我对安全的理解。API Key 不能通过 CLI 明文传入，也不能写入 `Context`、日志、SQLite 或测试产物。项目将凭据保存在系统 keyring 中，并限制明文能够到达的位置；Demo 模式不构造真实 `Provider`。

我原以为代码和测试通过就代表项目完成，后来才认识到还要构建 wheel 和 sdist、验证 CLI，并处理版本与平台差异。GitHub CI 曾因测试写死 Windows Python 路径而在 Linux 失败，这让我认识到不能只以本机为标准，应使用 `sys.executable` 等跨平台接口并尽早通过 CI 验证。

## 8. 如果重新实施

如果重新实施，我会先完成一个可以运行和演示的最小 MVP，再逐步补充安全、持久化和分发功能。我也会更早建立 CI 并添加 `.gitattributes`，尽早检查 Windows 与 Linux 兼容性，避免发布前才发现 Python 路径和 CRLF 问题。

## 9. 对 Superpowers 方法论的评价

Superpowers 最大的优点是规范开发过程。项目复杂度提高后，它能帮助开发者提前明确方法、范围和验证标准。但它并不适用于所有任务，头脑风暴可能引出无关问题，过细的流程也会扩大工作量。

Superpowers 还假设 `SPEC` 和测试能够正确表达需求、工具环境能够稳定运行。本项目既出现过测试期望错误，也多次受到 Windows 沙箱故障影响。因此，我认为 Superpowers 更适合作为可以根据项目规模裁剪的风险控制框架，而不是机械应用于每一次简单修改。

## AI Assistance Disclosure

Codex 参与了需求分析、实现计划、代码生成、测试、调试和文档整理。我负责需求与架构决策、权限控制、结果审核、Git 提交和发布授权。

本 Reflection 中的项目经历、个人感受、评价和观点由我本人提供。AI 协助整理文章结构、纠正技术概念、压缩篇幅和润色语言。
